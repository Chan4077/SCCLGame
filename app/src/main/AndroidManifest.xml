<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edricchan.scclgame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:noHistory="true"
            android:screenOrientation="landscape"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FlashCardGameActivity"
            android:label="@string/activity_flash_card_game_label"
            android:parentActivityName=".SelectionCardActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".SelectionCardActivity"
            android:label="@string/activity_selection_card_label"
            android:parentActivityName=".TopicCardActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".TopicCardActivity"
            android:label="@string/activity_topic_card_label"
            android:screenOrientation="landscape" />
    </application>

</manifest>